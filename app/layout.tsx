import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'Elevator Control System | Production Demo',
  description: 'Elementary elevator control system with intelligent dispatch algorithm, real-time simulation, and production-ready architecture',
  keywords: 'elevator, control system, simulation, TypeScript, Next.js, real-time',
  authors: [{ name: 'Tarang Shah' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}